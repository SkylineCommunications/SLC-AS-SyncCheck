# SLC-AS-SyncCheck
DataMiner Automation script that audits synchronization of Element, Service and RemoteService files across a DMA cluster — comparing each agent's on-disk folders against its Failover partner and against SLNet — and produces a per-run report grouped by DMA under `C:\Skyline_Data\SyncCheckResults\`.
